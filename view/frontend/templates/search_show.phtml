<?php
$helper         = $this->helper('Conceptive\AdvanceSearch\Helper\Data');
$popupEnabled   = $helper->getGeneralConfig('popup');
$popupEnabClass = ($popupEnabled == 1) ? "popup-modal" : null;
$linkLocation   = $helper->getLinkLocation();
$linklabel     = $helper->getGeneralConfig('link_label');
?>

<?php if ($linkLocation == 'header' && $block->getNameInLayout() == 'cc_search.header.link') : ?>
    <!-- Display link in the header -->
    <a href="<?= $block->getUrl('productfinder') ?>" class="ccsearch <?= /* @noEscape */ $popupEnabClass ?>"><?= /* @noEscape */ $linklabel ?></a>
<?php elseif ($linkLocation == 'footer' && $block->getNameInLayout() == 'cc_search.footer.link') : ?>
    <!-- Display link in the footer -->
    <a href="<?= $block->getUrl('productfinder') ?>" class="ccsearch <?= /* @noEscape */ $popupEnabClass ?>"><?= /* @noEscape */ $linklabel ?></a>
<?php endif; ?>